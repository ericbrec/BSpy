[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"
[project]
version = "1.0.3"
[tool.semantic_release]
version_variables = ["pyproject.toml:version"]